{
  "name": "@workspace/api-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'Streamlit runs from root via Streamlit App workflow' && exit 0",
    "build": "pip install -r ../../requirements.txt",
    "start": "cd ../.. && streamlit run app.py --server.address 0.0.0.0 --server.port 8080 --server.headless true --browser.gatherUsageStats false",
    "typecheck": "echo 'no typecheck needed' && exit 0"
  }
}
