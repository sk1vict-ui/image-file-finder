# 캡쳐 이미지 원본 파일 찾기 — 사용 매뉴얼

> 캡쳐(스크린샷) 이미지 한 장만 있으면, 그것이 어느 원본 파일 · 어느 페이지에서 나왔는지 자동으로 찾아주는 웹 도구입니다.

---

## 📑 목차

1. [접속하기](#1-접속하기)
2. [지원하는 파일 형식](#2-지원하는-파일-형식)
3. [사용 방법 (3단계)](#3-사용-방법-3단계)
4. [결과 보는 법](#4-결과-보는-법)
5. [팁 & 모범 사례](#5-팁--모범-사례)
6. [자주 묻는 질문](#6-자주-묻는-질문)
7. [문제 해결](#7-문제-해결)

---

## 1. 접속하기

### 본인이 사용할 때

배포 후 받은 URL을 브라우저에 입력하세요:

```
https://<프로젝트이름>.replit.app
```

- 북마크 추가 권장
- PC / 노트북 / 태블릿 / 모바일 모두 동일하게 동작
- 첫 접속 시 5~10초 정도 로딩 (이후엔 즉시)

### 다른 사람에게 공유할 때

위 URL을 **카톡 · 메일 · 메신저로 보내기만 하면 끝**입니다.

- ❌ 설치 불필요
- ❌ 회원가입 불필요
- ❌ 로그인 불필요
- ✅ 링크 클릭 → 바로 사용

---

## 2. 지원하는 파일 형식

### 🔍 캡쳐 이미지 (1단계에서 업로드)

| 형식 | 지원 |
|---|---|
| PNG | ✅ |
| JPG / JPEG | ✅ |
| WEBP | ✅ |

### 📚 원본 후보 파일 (2단계에서 업로드)

| 형식 | 지원 | 비고 |
|---|---|---|
| **PDF** | ✅ | 모든 페이지 검색 |
| **이미지** (PNG/JPG/JPEG/WEBP) | ✅ | 단일 이미지 비교 |
| **PPTX** (PowerPoint) | ✅ | 슬라이드 전체 렌더링 후 검색 |
| **DOCX** (Word) | ✅ | 페이지 전체 렌더링 후 검색 |
| **PPT** (구버전) | ✅ | 자동 변환 |
| **DOC** (구버전) | ✅ | 자동 변환 |
| ZIP 압축파일 | ✅ | 안에 든 위 형식들을 자동 추출 |

> 💡 한글(HWP), 엑셀(XLSX) 등은 현재 지원하지 않습니다. PDF로 변환해서 올려주세요.

---

## 3. 사용 방법 (3단계)

### 🟦 1단계: 찾고 싶은 캡쳐 이미지 업로드

페이지 상단의 **"캡쳐 이미지 파일을 선택하세요"** 버튼을 클릭하거나 파일을 끌어다 놓으세요.

- 한 번에 한 장만 올릴 수 있습니다
- 업로드되면 미리보기가 표시됩니다
- 잘못 올렸으면 다시 선택하면 교체됩니다

### 🟩 2단계: 원본 후보 파일 추가

찾고 싶은 캡쳐가 있을 만한 **원본 파일들**을 모두 추가하세요. 네 가지 방법이 있습니다:

#### 📄 탭 ① 파일로 추가
- **여러 파일을 한 번에**: Ctrl(또는 ⌘)+클릭, Shift+클릭으로 다중 선택
- 가장 일반적인 방법

#### 📁 탭 ② 폴더 통째로 추가
- 폴더 안의 **모든 파일(하위 폴더 포함)**을 한 번에 업로드
- 큰 폴더에서 검색할 때 가장 빠름
- Chrome / Edge / Safari에서만 지원 (Firefox ❌)

#### 🗜️ 탭 ③ ZIP으로 추가
- 미리 ZIP으로 묶은 파일을 올리면 자동 압축 해제
- 모바일이나 네트워크가 느릴 때 효율적

#### 📂 탭 ④ 경로 지정 (로컬 실행 시만)
- 배포 버전에서는 사용 불가 (포터블 버전 전용)

> ✅ **같은 파일을 두 번 추가해도 자동으로 중복 제거됩니다.**  
> ✅ **여러 탭을 섞어서 사용해도 됩니다.** (예: 파일 5개 + ZIP 1개)

추가된 후보 파일 목록은 아래에 표시되며, ❌ 버튼으로 개별 삭제 가능합니다.

### 🟥 3단계: 원본 찾기 실행

**"원본 찾기 실행"** 버튼을 누르면 분석이 시작됩니다.

- 진행률 바가 실시간으로 표시됩니다
- 파일 수와 크기에 따라 수 초 ~ 수 분 소요
- 끝나면 결과가 자동으로 표시됩니다

---

## 4. 결과 보는 법

분석이 끝나면 다음 정보가 표시됩니다:

| 항목 | 의미 |
|---|---|
| **🏆 가장 유사한 원본** | 캡쳐와 일치한 파일 이름 |
| **📄 페이지 번호** | PDF / PPTX / DOCX에서 몇 페이지인지 |
| **📊 유사도 점수** | 높을수록 일치도 높음 (참고용) |
| **🔴 매칭 영역 (빨간 박스)** | 원본 페이지에서 캡쳐된 정확한 위치 |

### 점수 해석 가이드

| 점수 범위 | 의미 |
|---|---|
| 매우 높음 | 거의 확실한 매칭 — 같은 자료에서 캡쳐된 것 |
| 중간 | 비슷한 자료 — 디자인이 유사하거나 일부만 일치 |
| 낮음 | 매칭 후보가 약함 — 후보 파일에 원본이 없을 수 있음 |

> 💡 **매칭 영역(빨간 박스)이 캡쳐와 시각적으로 일치하는지** 직접 눈으로 확인하는 것이 가장 정확합니다.

---

## 5. 팁 & 모범 사례

### ✨ 정확도를 높이려면
- 캡쳐 이미지는 **원본 해상도** 그대로 사용 (축소된 썸네일보다 정확함)
- 캡쳐에 **고유한 텍스트/그림**이 많이 포함되어 있을수록 잘 찾음
- 단색 배경이나 매우 일반적인 차트는 오인식 가능성 있음

### ⚡ 속도를 높이려면
- 후보 파일이 100개 넘으면 **ZIP으로 묶어서 올리기** (업로드 자체가 빨라짐)
- 명백히 무관한 파일은 미리 제외
- 큰 PDF(100+ 페이지)는 머신을 2 vCPU로 업그레이드 권장

### 🔒 프라이버시
- 업로드된 파일은 **처리 직후 서버에서 자동 삭제**됩니다
- 다른 사용자에게 공유되지 않습니다 (세션 격리)
- 검색 결과도 화면을 새로고침하면 사라집니다

---

## 6. 자주 묻는 질문

**Q. 여러 명이 동시에 써도 되나요?**  
A. 네. 각자의 세션이 격리되어 서로 영향 없습니다.

**Q. 캡쳐가 살짝 잘려 있어도 찾을 수 있나요?**  
A. 네. 부분 매칭 알고리즘이라 일부만 일치해도 찾습니다.

**Q. 캡쳐를 90도 회전시켜도 되나요?**  
A. 현재 회전된 이미지는 정확도가 낮아집니다. 원래 방향으로 돌려서 올리세요.

**Q. 한 번에 올릴 수 있는 최대 용량은?**  
A. 파일당 200MB. 더 큰 파일은 ZIP으로 묶어서 올리면 됩니다.

**Q. 모바일에서도 잘 되나요?**  
A. 네. 단, 폴더 통째로 업로드는 PC에서만 가능합니다.

**Q. 인터넷이 안 되는 곳에서도 쓸 수 있나요?**  
A. 웹 버전은 불가. 오프라인 사용은 **포터블 버전**(`README_PORTABLE.md` 참고)을 사용하세요.

**Q. 비용은 얼마나 드나요?**  
A. Replit Autoscale 기준 — 사용한 시간만큼만 과금. 가끔 쓰면 월 몇백~몇천 원 수준.

**Q. URL을 바꾸거나 도메인을 연결할 수 있나요?**  
A. Replit Publishing 설정에서 커스텀 도메인 연결 가능합니다.

---

## 7. 문제 해결

### "Application failed to respond"
- 배포가 아직 진행 중일 수 있음 → 5분 후 재시도
- 그래도 안 되면 Replit의 배포 로그 확인

### 페이지 로딩이 너무 느림
- 첫 접속은 5~10초 정상
- 그 이상 걸리면 머신 사양 업그레이드 (2 vCPU / 4GB)

### 파일 업로드가 실패함
- 200MB 초과 여부 확인
- 인터넷 연결 불안정 시 작은 파일부터 시도
- ZIP으로 묶어서 재시도

### 매칭 결과가 이상함
- 캡쳐 이미지가 흐릿하거나 너무 작은지 확인
- 후보 파일에 실제로 원본이 들어있는지 확인
- 점수가 매우 낮으면 후보가 부정확하다는 신호

### 🟡 포터블 모드라고 나옴 (배포 환경에서)
- 배포 빌드에 LibreOffice가 빠진 경우
- `.pptx / .docx`는 계속 동작
- `.ppt / .doc`만 영향 받음 → 파일을 `.pptx / .docx`로 저장 후 재시도

### 빨간 박스가 안 그려짐
- 매칭 영역을 특정할 만큼 충분히 일치하지 않은 경우
- 점수는 높은데 박스가 없으면 → 전체적으로 비슷하지만 정확한 위치는 불분명

---

## 📞 추가 도움

- 오프라인/USB 사용: `README_PORTABLE.md` 참고
- 코드 수정 후 재배포: Replit Publishing → **Republish** 클릭 (URL 그대로 유지)
- 더 많은 기능 요청: Replit 작업 환경에서 추가 개발

---

**Happy Searching! 🔍**
